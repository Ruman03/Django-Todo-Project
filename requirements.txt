# vercel.json
{
  "version": 2,
  "builds": [
    {
      "src": "mytodo/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "mytodo/wsgi.py"
    }
  ]
}

Django>=3.2,<4.0
whitenoise==6.6.0